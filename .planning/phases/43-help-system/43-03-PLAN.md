---
phase: 43-help-system
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/content/help/elements.ts
  - src/content/help/general.ts
autonomous: true

must_haves:
  truths:
    - "Help content exists for core element types (Knob, Slider, Button, Label, Meter)"
    - "Help content exists for container types (Panel, Frame, GroupBox, Collapsible)"
    - "Help content exists for display types (NumericDisplay, LED variants)"
    - "Help content exists for specialized types (visualizations, curves, meters)"
    - "General help content provides app overview and keyboard shortcuts"
  artifacts:
    - path: "src/content/help/elements.ts"
      provides: "Help content for all element types"
      exports: ["elementHelp", "getElementHelp"]
    - path: "src/content/help/general.ts"
      provides: "General app help for F1 with no selection"
      exports: ["generalHelp"]
  key_links:
    - from: "src/content/help/elements.ts"
      to: "src/content/help/types.ts"
      via: "HelpContent type import"
      pattern: "HelpContent"
    - from: "src/hooks/useHelpShortcut.ts"
      to: "src/content/help/elements.ts"
      via: "getElementHelp import (plan 43-04 consumer)"
      pattern: "getElementHelp"
      note: "Forward reference - validates F1 shortcut wiring in plan 43-04"
---

<objective>
Create comprehensive help content for all element types and general app help.

Purpose: Enable users to access documentation for any element type via F1 shortcut (plan 04) or future element-specific help buttons. This covers requirements HELP-05, HELP-07.

Output:
- elementHelp object with content for all ~100+ element types
- getElementHelp utility function for type-safe access
- generalHelp for app-level documentation
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/43-help-system/43-RESEARCH.md
@.planning/phases/43-help-system/43-01-SUMMARY.md

# Element types reference
@src/types/elements/index.ts
@src/types/elements/controls.ts
@src/types/elements/displays.ts
@src/types/elements/containers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create general app help</name>
  <files>src/content/help/general.ts</files>
  <action>
Create general help content shown when F1 pressed with no element selected:

1. **Import** HelpContent from './types'

2. **Export generalHelp** as HelpContent:

```typescript
export const generalHelp: HelpContent = {
  title: 'Faceplate - VST3 UI Designer',
  description: 'Design and export VST3 plugin user interfaces with visual drag-and-drop tools and automatic code generation for JUCE WebView2.',
  examples: [
    {
      label: 'Getting Started',
      explanation: 'Add elements from the left palette by dragging them onto the canvas. Position and resize using drag handles or arrow keys. Customize properties in the right panel. Export to C++ code for JUCE integration.'
    },
    {
      label: 'Keyboard Shortcuts',
      explanation: 'Ctrl/Cmd+C/V: Copy/Paste | Ctrl/Cmd+Z/Y: Undo/Redo | Arrow keys: Nudge 1px | Shift+Arrow: Nudge 10px | Ctrl+G: Toggle grid | Delete: Remove element | F1: Context help'
    },
    {
      label: 'Workflow',
      explanation: '1. Design UI on canvas with elements from palette\\n2. Organize elements using Layers panel\\n3. Customize appearance in Properties panel\\n4. Preview in browser to test\\n5. Export C++ code for JUCE WebView2'
    },
    {
      label: 'Multi-Window Projects',
      explanation: 'Create multiple windows for complex plugins (main UI, settings, about). Use Window tabs to switch between them. Buttons can navigate between windows at runtime.'
    }
  ],
  relatedTopics: [
    'Select an element and press F1 for element-specific help',
    'Each property section has a (?) button for detailed help',
    'Use Layers panel to organize and lock/hide elements',
    'Export to folder for complete JUCE integration bundle'
  ]
}
```
  </action>
  <verify>File exists at src/content/help/general.ts. Export generalHelp. TypeScript compiles.</verify>
  <done>generalHelp exported with comprehensive app overview and shortcuts</done>
</task>

<task type="auto">
  <name>Task 2: Create element help content</name>
  <files>src/content/help/elements.ts</files>
  <action>
Create help content for all element types. This is a large file - organize by category.

1. **Import** HelpContent from './types'

2. **Create category-grouped content objects** (for organization, not separate exports):

**Core Controls:**
- knob, slider, button, meter, label, image
- steppedknob, centerdetentknob, dotindicatorknob
- bipolarslider, crossfadeslider, notchedslider, arcslider, multislider, rangeslider

**Buttons & Switches:**
- iconbutton, kickbutton, toggleswitch, powerbutton
- rockerswitch, rotaryswitch, segmentbutton

**Value Displays:**
- numericdisplay, timedisplay, percentagedisplay, ratiodisplay
- notedisplay, bpmdisplay, editabledisplay, multivaluedisplay

**LEDs:**
- singleled, bicolorled, tricolorled, ledarray, ledring, ledmatrix

**Professional Meters:**
- rmsmetermo, rmsmeterstereo, vumetermono, vumeterstereo
- ppmtype1mono, ppmtype1stereo, ppmtype2mono, ppmtype2stereo
- truepeakmetermono, truepeakmeterstereo
- lufsmomomo, lufsmomostereo, lufsshortmono, lufsshortstereo, lufsintmono, lufsintstereo
- k12metermono through k20meterstereo
- correlationmeter, stereowidthmeter, gainreductionmeter

**Containers:**
- panel, frame, groupbox, collapsible
- tooltip, windowchrome, horizontalspacer, verticalspacer

**Form Controls:**
- dropdown, checkbox, radiogroup, textfield
- multiselectdropdown, combobox, menubutton

**Navigation:**
- stepper, breadcrumb, tabbar, tagselector, treeview

**Visualizations:**
- scrollingwaveform, spectrumanalyzer, spectrogram, goniometer, vectorscope
- waveform, oscilloscope

**Curves:**
- eqcurve, compressorcurve, envelopedisplay, lfodisplay, filterresponse

**Specialized Audio:**
- pianokeyboard, drumpad, padgrid, stepsequencer, xypad
- wavetabledisplay, harmoniceditor, envelopeeditor
- sampledisplay, looppoints, patchbay, signalflow
- modulationmatrix, presetbrowser

**Decorative:**
- rectangle, line, svggraphic, dbdisplay, frequencydisplay

3. **Merge into single elementHelp object**:
```typescript
export const elementHelp: Record<string, HelpContent> = {
  // Core controls
  knob: { ... },
  slider: { ... },
  // ... all types
}
```

4. **Create getElementHelp utility**:
```typescript
export function getElementHelp(elementType: string): HelpContent {
  return elementHelp[elementType] || {
    title: `${elementType} Element`,
    description: `A ${elementType} element. Select and press F1 for help, or check the Properties panel sections.`,
    relatedTopics: ['Position & Size properties', 'Identity properties', 'Lock to prevent changes']
  }
}
```

**Content guidelines for each element:**
- title: "{Element Name} Element" (e.g., "Knob Element")
- description: 1-2 sentences explaining what it is and its primary use case
- examples: 2-4 practical examples covering common usage
- relatedTopics: 2-4 related elements or features

**Example for Knob:**
```typescript
knob: {
  title: 'Knob Element',
  description: 'A rotary control for continuous parameters like gain, frequency, or time values. Rotates from minimum (bottom-left) to maximum (top-right).',
  examples: [
    {
      label: 'Basic Usage',
      explanation: 'Drag from palette, position on canvas. Set min/max values and default position in Properties. Bind to JUCE parameter via Parameter ID.'
    },
    {
      label: 'Custom Appearance',
      explanation: 'Change colors for indicator, track, and fill. Use SVG export to create custom graphics in vector tools, then import back for validation.'
    },
    {
      label: 'Parameter Binding',
      explanation: 'Set Parameter ID to match your JUCE AudioParameter. At runtime, the knob automatically reflects and controls the parameter value.'
    }
  ],
  relatedTopics: [
    'Use SteppedKnob for discrete values with detents',
    'Use CenterDetentKnob for bipolar parameters',
    'Export SVG to create branded knob designs'
  ]
}
```

**Coverage tiers:**
- **Tier 1 (full docs):** Core controls (knob, slider, button, meter, label, image), containers (panel, frame, groupbox, collapsible), common displays (numericdisplay, singleled) - ~15 types with full examples
- **Tier 2 (standard docs):** All remaining types (~85+) with title, description (1-2 sentences), and relatedTopics (no examples array)
- **Minimum total:** 70+ explicit entries in elementHelp object
- **Fallback:** getElementHelp returns generic content for any unlisted types
  </action>
  <verify>
1. File exists at src/content/help/elements.ts
2. Exports elementHelp and getElementHelp
3. TypeScript compiles without errors
4. Object.keys(elementHelp).length >= 70 (verify with console or test)
5. Tier 1 elements (knob, slider, button, meter, label, panel, frame, groupbox, numericdisplay, singleled) have examples array
6. getElementHelp('unknowntype') returns fallback content
  </verify>
  <done>elementHelp contains 70+ element types (15 with full docs, 55+ with standard docs), getElementHelp provides fallback for unknown types</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. elementHelp object has 70+ entries: `Object.keys(elementHelp).length >= 70`
3. Tier 1 coverage (full docs with examples):
   - Core controls: knob, slider, button, label, meter
   - Containers: panel, frame, groupbox, collapsible
   - Displays: numericdisplay, singleled
4. Tier 2 coverage (all listed categories represented):
   - Professional meters: at least rmsmetermo, vumetermono
   - Visualizations: spectrumanalyzer
   - Specialized: pianokeyboard, stepsequencer
5. getElementHelp('knob') returns detailed content with examples
6. getElementHelp('unknowntype') returns generic fallback
7. generalHelp has useful app overview and shortcuts
</verification>

<success_criteria>
- elementHelp object contains 70+ explicit entries
- 15+ Tier 1 elements have full documentation with examples array
- All remaining types have at minimum title, description, relatedTopics
- Content explains what elements do and how to use them
- Step-by-step instructions provided for Tier 1 elements (HELP-07)
- getElementHelp provides graceful fallback for any type
- generalHelp gives users a solid introduction to the tool
</success_criteria>

<output>
After completion, create `.planning/phases/43-help-system/43-03-SUMMARY.md`
</output>
