---
phase: 50-rebranding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/helpService.ts
  - src/components/Layout/HelpPanel.tsx
  - src/services/export/jsGenerator.ts
  - docs/FACEPLATE_SYNC_GUIDE.md
  - docs/FACEPLATE_DOCUMENTATION.md
  - docs/WORKFLOW.md
  - .claude/commands/generate-ui.md
  - package-lock.json
autonomous: true

must_haves:
  truths:
    - "Case-insensitive search for 'allthecode' returns 0 results in src/, docs/, .claude/"
    - "Search for 'vst3-webview-ui-designer' returns 0 results in src/, docs/, .claude/"
    - "GitHub issue URLs point to AllTheMachines/Faceplate"
    - "Export JS comment header says 'Faceplate' not old project name"
  artifacts:
    - path: "src/services/helpService.ts"
      provides: "GitHub issue URLs"
      contains: "AllTheMachines/Faceplate"
    - path: "src/components/Layout/HelpPanel.tsx"
      provides: "Bug report URL"
      contains: "AllTheMachines/Faceplate"
    - path: "src/services/export/jsGenerator.ts"
      provides: "Export header comment"
      contains: "Faceplate"
    - path: "docs/FACEPLATE_DOCUMENTATION.md"
      provides: "User documentation"
      contains: "AllTheMachines/Faceplate"
  key_links:
    - from: "src/services/helpService.ts"
      to: "GitHub Issues API"
      via: "URL template strings"
      pattern: "github\\.com/AllTheMachines/Faceplate"
---

<objective>
Complete rebrand by replacing all remaining references to 'allthecode' and 'vst3-webview-ui-designer' with 'AllTheMachines' and 'Faceplate' respectively.

Purpose: Establish consistent branding across the entire codebase before implementing Pro licensing features.
Output: All source code, documentation, and configuration files reference the new brand names.
</objective>

<execution_context>
@C:\Users\User\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\User\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-rebranding/50-RESEARCH.md

Key research findings:
- Git remote URL and package.json already updated (commit 149811f)
- Remaining work: src/ files (GitHub URLs), docs/ files, .claude/ commands
- NEVER manually edit package-lock.json - run npm install to regenerate
- Use case-insensitive replacement for brand names
- Single atomic commit for entire rebrand
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all brand name references</name>
  <files>
    src/services/helpService.ts
    src/components/Layout/HelpPanel.tsx
    src/services/export/jsGenerator.ts
    docs/FACEPLATE_SYNC_GUIDE.md
    docs/FACEPLATE_DOCUMENTATION.md
    docs/WORKFLOW.md
    .claude/commands/generate-ui.md
  </files>
  <action>
Replace brand references using these exact transformations:

1. **GitHub URLs** (case-sensitive, exact match):
   - `github.com/allthecodeDev/vst3-webview-ui-designer` -> `github.com/AllTheMachines/Faceplate`
   - `raw.githubusercontent.com/allthecodeDev/vst3-webview-ui-designer` -> `raw.githubusercontent.com/AllTheMachines/Faceplate`

2. **Project name in comments/text** (case-sensitive):
   - `vst3-webview-ui-designer` -> `Faceplate` (when referring to project name)

3. **Author/org name** (case-insensitive):
   - `allthecodeDev` -> `AllTheMachines`
   - `allthecode` -> `AllTheMachines`

Files and specific changes:

**src/services/helpService.ts** (lines 124, 153):
- Change `https://github.com/allthecodeDev/vst3-webview-ui-designer/issues/new` to `https://github.com/AllTheMachines/Faceplate/issues/new`

**src/components/Layout/HelpPanel.tsx** (line 112):
- Change `https://github.com/allthecodeDev/vst3-webview-ui-designer/issues/new` to `https://github.com/AllTheMachines/Faceplate/issues/new`

**src/services/export/jsGenerator.ts** (line 308):
- Change `// AUTO-GENERATED by vst3-webview-ui-designer` to `// AUTO-GENERATED by Faceplate`

**docs/FACEPLATE_SYNC_GUIDE.md**:
- Update all GitHub URLs from `allthecodeDev/vst3-webview-ui-designer` to `AllTheMachines/Faceplate`
- Update all raw.githubusercontent.com URLs similarly

**docs/FACEPLATE_DOCUMENTATION.md**:
- Update clone URL, issues URL, discussions URL
- Update author from `allthecodeDev` to `AllTheMachines`
- Update directory reference in project structure section

**docs/WORKFLOW.md**:
- Update clone URL example

**.claude/commands/generate-ui.md**:
- Update project reference from `vst3-webview-ui-designer project` to `Faceplate project`

Do NOT modify:
- package.json (already updated in commit 149811f)
- .git/config (already updated)
- package-lock.json (will be regenerated in Task 2)
- .planning/ files (historical context is acceptable)
- Absolute local paths like `D:\___ATM\vst3-webview-ui-designer\` (local filesystem, not repo name)
  </action>
  <verify>
Run case-insensitive search for old brand names in modified directories:
```bash
rg -i "allthecode" src/ docs/ .claude/commands/ --files-with-matches
rg "vst3-webview-ui-designer" src/ docs/ .claude/commands/ --files-with-matches
```
Both commands should return no results.
  </verify>
  <done>
All source code, documentation, and Claude commands reference 'AllTheMachines/Faceplate' instead of 'allthecodeDev/vst3-webview-ui-designer'. GitHub URLs point to new repository location.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate package-lock.json and verify</name>
  <files>
    package-lock.json
  </files>
  <action>
1. Run `npm install` to regenerate package-lock.json with the new package name
   - package.json name is already 'faceplate' (from commit 149811f)
   - npm install will update package-lock.json to match

2. Verify the lockfile is valid:
   - Run `npm install --dry-run` to check for errors
   - Check that package-lock.json `name` field is 'faceplate'

3. Run final verification:
   - `rg -i "allthecode" --files-with-matches` (should return only .planning/ files if any)
   - `rg "vst3-webview-ui-designer" --files-with-matches` (should return only .planning/ files and local paths)
   - Verify src/, docs/, .claude/ directories are clean

4. Update buildInfo.ts with current timestamp per CLAUDE.md instructions:
   - Run `date "+%d %b %H:%M CET"` to get exact time
   - Update src/buildInfo.ts with the result
  </action>
  <verify>
```bash
# Verify package-lock.json name field
grep '"name": "faceplate"' package-lock.json | head -1

# Verify no old references in source/docs
rg -i "allthecode" src/ docs/ .claude/commands/ 2>/dev/null | wc -l
rg "vst3-webview-ui-designer" src/ docs/ .claude/commands/ 2>/dev/null | wc -l

# Both counts should be 0
```
  </verify>
  <done>
package-lock.json regenerated with 'faceplate' name. No old brand references remain in src/, docs/, or .claude/commands/. buildInfo.ts updated with current timestamp.
  </done>
</task>

</tasks>

<verification>
Final verification checklist:

1. **Brand name search** (must all return 0 for src/docs/.claude):
   ```bash
   rg -i "allthecode" src/ docs/ .claude/commands/ --files-with-matches | wc -l
   rg "vst3-webview-ui-designer" src/ docs/ .claude/commands/ --files-with-matches | wc -l
   ```

2. **Package integrity**:
   ```bash
   npm install --dry-run
   # Should complete without errors
   ```

3. **GitHub URLs work**:
   - Check that `https://github.com/AllTheMachines/Faceplate` is valid
   - (Manual verification - URL should not 404)

4. **Build still works**:
   ```bash
   npm run build
   # Should complete without errors
   ```
</verification>

<success_criteria>
1. `rg -i "allthecode" src/ docs/ .claude/commands/` returns no matches
2. `rg "vst3-webview-ui-designer" src/ docs/ .claude/commands/` returns no matches
3. `npm install` completes without errors
4. `npm run build` completes without errors
5. package-lock.json contains `"name": "faceplate"`
6. All GitHub URLs in source code point to `AllTheMachines/Faceplate`
</success_criteria>

<output>
After completion, create `.planning/phases/50-rebranding/50-01-SUMMARY.md`
</output>
