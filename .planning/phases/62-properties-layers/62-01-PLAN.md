---
phase: 62-properties-layers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/manual/properties.md
autonomous: true

must_haves:
  truths:
    - "Properties panel topic file documents common properties: position (x, y), size (width, height), name, parameterId, lock"
    - "Parameter Binding section explains what parameterId IS, gives concrete volume-slider-to-gain example, shows C++ AudioParameterFloat snippet, and includes text flow diagram"
    - "Element-specific property sections use 3-column tables (property, type, description) organized by category with representative examples for knob, slider, button, and meter"
    - "Help System subsection documents (?) button mechanism on property sections and F1 contextual help shortcut"
    - "No cross-references to common properties within category sections"
    - "Screenshot placeholders exist for: properties panel overview, parameterId field, help panel open"
  artifacts:
    - path: "docs/manual/properties.md"
      provides: "Complete properties panel documentation with common props, parameter binding, element-specific tables, help system"
      min_lines: 200
  key_links:
    - from: "docs/manual/properties.md"
      to: "docs/manual/README.md"
      via: "navigation link back to index"
      pattern: "\\[.*\\]\\(README\\.md\\)"
    - from: "docs/manual/properties.md"
      to: "docs/ELEMENT_REFERENCE.md"
      via: "reference link for full property listings"
      pattern: "\\[.*\\]\\(\\.\\./ELEMENT_REFERENCE\\.md\\)"
    - from: "docs/manual/properties.md"
      to: "docs/manual/canvas.md"
      via: "cross-reference to canvas topic for element selection"
      pattern: "\\[.*\\]\\(canvas\\.md\\)"
---

<objective>
Write the complete properties panel documentation for the Faceplate user manual.

Purpose: Give users a reference guide to the properties panel -- what common properties every element has, how parameterId bridges UI to JUCE audio processing, what element-specific properties exist by category, and how the built-in help system works.
Output: `docs/manual/properties.md` -- a complete properties panel reference satisfying PROP-01 through PROP-05.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-properties-layers/62-RESEARCH.md
@.planning/phases/62-properties-layers/62-CONTEXT.md
@docs/manual/getting-started.md
@docs/manual/canvas.md
@docs/manual/README.md
@docs/ELEMENT_REFERENCE.md
@src/components/Properties/PropertyPanel.tsx
@src/components/Properties/PropertySection.tsx
@src/components/Properties/KnobProperties.tsx
@src/components/Properties/SliderProperties.tsx
@src/components/Properties/ButtonProperties.tsx
@src/components/Properties/MeterProperties.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the properties panel documentation</name>
  <files>docs/manual/properties.md</files>
  <action>
Create `docs/manual/properties.md` following the established topic file format from `getting-started.md` and `canvas.md`: title, overview paragraph, then content sections.

**IMPORTANT:** This is documentation only. No code changes. Target audience is beginner-friendly. This is a REFERENCE document, NOT a tutorial. Do NOT include introductory steps like "first select an element" -- that is covered in canvas.md.

**CRITICAL CONTEXT DECISIONS (from 62-CONTEXT.md -- these are LOCKED):**
- Reference-only format -- no walkthrough intro
- Common properties documented once at the top in a dedicated section
- Element-specific properties organized by category with 3-column tables: property name, type, brief description
- No cross-references or reminders about common props in each category section
- parameterId documented as freeform string field (no prescriptive naming conventions)
- Help system is a brief subsection, not its own major section
- Don't list which sections have help content -- users discover naturally

The file must contain these sections in this order:

**1. Title and overview (2 sentences max)**
```
# Properties Panel

The Properties panel on the right side of the interface shows the settings for the currently selected element. Select any element on the canvas to view and edit its properties here.
```

Screenshot placeholder: `![Properties panel showing settings for a selected knob element](../images/properties-panel-overview.png)`

**2. Section: Common Properties** (anchored as `## Common Properties`)

Opening line: All elements share these properties regardless of type.

Then a subsection for each property group as shown in PropertyPanel.tsx:

**### Position & Size**

| Property | Type | Description |
|----------|------|-------------|
| X | number | Horizontal position on the canvas (pixels) |
| Y | number | Vertical position on the canvas (pixels) |
| Width | number | Element width (pixels, minimum 20) |
| Height | number | Element height (pixels, minimum 20) |

**### Identity**

| Property | Type | Description |
|----------|------|-------------|
| Name | text | Display name for the element (becomes the HTML ID in exported code) |
| Parameter ID | text | Optional binding to a JUCE audio parameter (see [Parameter Binding](#parameter-binding) below) |

**### Lock**

Brief explanation: Check **Lock element** to prevent the element from being moved or resized on the canvas. Locked elements can still have their properties edited in this panel.

**### SVG Export**

Brief explanation: For elements that support it, an **Export** button lets you download the element as an SVG file with named layers. This is useful for creating custom element styles (see [Element Styles](styles.md)).

**3. Section: Parameter Binding** (anchored as `## Parameter Binding`)

This is the conceptual explanation section per CONTEXT decisions. Structure:

**Opening paragraph:** The **Parameter ID** field is the bridge between a UI element in Faceplate and an audio parameter in your JUCE plugin. When you export your design, each element's Parameter ID tells the generated JavaScript which JUCE parameter to control.

**Concrete example:** For example, if you have a knob that controls volume, you would set its Parameter ID to `gain`. In your JUCE plugin, you would define an audio parameter with the same name. When the user turns the knob in the exported UI, the value is sent to the `gain` parameter in your audio processor.

**Text flow diagram:**
```
Faceplate Element  -->  parameterId  -->  Exported JavaScript  -->  JUCE WebView2  -->  AudioProcessor
     (UI knob)          ("gain")       (setParameter call)      (native function)    (DSP parameter)
```

**C++ code snippet (brief, from RESEARCH.md):**
```cpp
// PluginProcessor.cpp - Define audio parameter
params.push_back(std::make_unique<juce::AudioParameterFloat>(
    "gain",           // Must match Faceplate's Parameter ID
    "Gain",
    0.0f, 1.0f, 0.5f
));
```

**Closing note:** The Parameter ID is a freeform text field -- it must match the parameter name defined in your JUCE C++ code exactly. Any element that the user interacts with (knobs, sliders, buttons, switches) can have a Parameter ID. Display-only elements like meters and labels typically do not need one.

Screenshot placeholder: `![Parameter ID field in the Identity section of the properties panel](../images/properties-parameter-id-field.png)`

**4. Section: Element-Specific Properties** (anchored as `## Element-Specific Properties`)

Opening: Beyond the common properties, each element type has its own settings. This section covers the most common property patterns by category. For a complete listing of every element's properties, see the [Element Reference](../ELEMENT_REFERENCE.md).

Then use category subsections with 3-column tables. Include REPRESENTATIVE properties per CONTEXT (not exhaustive -- that's what ELEMENT_REFERENCE is for). Category structure should match the palette categories relevant to properties:

**### Rotary Controls (Knobs)**

Representative properties from KnobProperties.tsx:

| Property | Type | Description |
|----------|------|-------------|
| min | number | Minimum value (default: 0) |
| max | number | Maximum value (default: 1) |
| value | number | Current value |
| startAngle | number | Arc start angle in degrees (default: -135) |
| endAngle | number | Arc end angle in degrees (default: 135) |
| style | select | Visual style: arc, filled, dot, or line |
| trackColor | color | Color of the knob track |
| fillColor | color | Color of the filled arc |
| showLabel | boolean | Show text label near the knob |
| showValue | boolean | Show current value readout |
| styleId | select | Custom SVG style from Element Styles |

**### Linear Controls (Sliders)**

Representative properties from SliderProperties.tsx:

| Property | Type | Description |
|----------|------|-------------|
| orientation | select | Vertical or horizontal |
| min | number | Minimum value |
| max | number | Maximum value |
| value | number | Current value |
| trackColor | color | Color of the slider track |
| trackFillColor | color | Color of the filled portion |
| thumbColor | color | Color of the draggable thumb |
| showLabel | boolean | Show text label |
| showValue | boolean | Show current value readout |
| styleId | select | Custom SVG style from Element Styles |

**### Buttons & Switches**

Representative properties from ButtonProperties.tsx:

| Property | Type | Description |
|----------|------|-------------|
| label | text | Button text |
| action | select | Click behavior: toggle, momentary, or navigate |
| targetWindowId | select | Target window for navigate action |
| backgroundColor | color | Button background color |
| textColor | color | Button text color |
| borderWidth | number | Border thickness in pixels |
| borderRadius | number | Corner rounding in pixels |
| fontSize | number | Label font size |
| fontFamily | select | Label font family |
| styleId | select | Custom SVG style from Element Styles |

**### Meters**

Representative properties from MeterProperties.tsx:

| Property | Type | Description |
|----------|------|-------------|
| orientation | select | Vertical or horizontal |
| value | number | Current meter level (0-1) |
| meterColor | color | Main meter fill color |
| backgroundColor | color | Meter background color |
| peakHold | boolean | Show peak hold indicator |
| peakHoldTime | number | Peak hold duration in milliseconds |
| segments | number | Number of meter segments (0 for smooth) |
| styleId | select | Custom SVG style from Element Styles |

**### Displays**

Brief note: Display elements show values but are not interactive. Common display properties include font settings (family, size, weight, color), value format, and decimal places. See the [Element Reference](../ELEMENT_REFERENCE.md) for properties of specific display types.

**### Containers**

Brief note: Container elements group other elements together. Container properties include background color, border settings, padding, and overflow behavior. Elements inside containers can be edited by double-clicking the container or using the **Edit Contents** button. See the [Element Reference](../ELEMENT_REFERENCE.md) for details.

**5. Section: Help System** (anchored as `## Help`)

Brief subsection per CONTEXT decisions (mechanism only, don't list which sections have help):

Each section in the Properties panel has a **(?)** button in its header. Click this button to open a help panel with detailed documentation about that section's properties.

You can also press **F1** while an element is selected to open contextual help for that element type. The help panel includes **Related Topics** links at the bottom for navigating to related documentation.

Screenshot placeholder: `![Help panel open showing property documentation and Related Topics links](../images/properties-help-panel-open.png)`

**6. Footer: navigation links**

At the end of the file, include:
```
---

[Back to User Manual](README.md) | [Canvas](canvas.md) | [Element Palette](palette.md) | [Layers](layers.md)
```

**Format rules (same as getting-started.md and canvas.md):**
- Use bold for UI element names: **Properties** panel, **Parameter ID** field, **Lock element** checkbox
- Use backticks for literal values and code: `gain`, `parameterId`, `0.5`
- Screenshot placeholder format: `![description](../images/filename.png)`
- Do NOT reference React components, TypeScript types, Zustand store, or implementation details
- Do NOT include numbered tutorial steps in this reference document (except in Parameter Binding example)
- Do NOT repeat common property information within category tables
- Link to ELEMENT_REFERENCE.md for full property details
  </action>
  <verify>
Verify the file exists and covers all requirements:
- Check: `docs/manual/properties.md` exists
- Check: Contains ## Common Properties section with Position & Size and Identity subsections (PROP-01, PROP-02)
- Check: Common properties include x, y, width, height, name, parameterId, rotation (PROP-02)
- Check: Contains ## Parameter Binding section with conceptual explanation, concrete example, C++ snippet, and flow diagram (PROP-04)
- Check: Contains ## Element-Specific Properties section with category tables (PROP-01, PROP-03)
- Check: Category tables use 3-column format: Property | Type | Description
- Check: Representative categories present: Rotary Controls, Linear Controls, Buttons & Switches, Meters (PROP-03)
- Check: Contains ## Help section documenting (?) buttons and F1 shortcut (PROP-05)
- Check: Screenshot placeholders exist for: properties-panel-overview.png, properties-parameter-id-field.png, properties-help-panel-open.png
- Check: Contains link to ../ELEMENT_REFERENCE.md for full property details
- Check: Contains link back to README.md
- Check: Contains cross-references to canvas.md, palette.md, layers.md
- Check: File is at least 200 lines
- Check: No code-level references (no React, TypeScript, Zustand mentions)
- Check: No cross-references to common props within category sections
  </verify>
  <done>
docs/manual/properties.md exists with:
- Common Properties section documenting position, size, name, parameterId, lock (PROP-01, PROP-02)
- Parameter Binding section with conceptual explanation, volume/gain example, C++ snippet, text flow diagram (PROP-04)
- Element-Specific Properties with 3-column tables for rotary controls, linear controls, buttons, meters (PROP-01, PROP-03)
- Help subsection documenting (?) buttons and F1 shortcut (PROP-05)
- 3 screenshot placeholders (overview, parameterId, help panel)
- Links to ELEMENT_REFERENCE.md, README.md, canvas.md, palette.md, layers.md
- 200+ lines, reference format, beginner-friendly, no implementation references
  </done>
</task>

</tasks>

<verification>
- `docs/manual/properties.md` exists and follows topic file format from getting-started.md
- All 5 PROP requirements satisfied (PROP-01 through PROP-05)
- Common properties documented once at top, not repeated in category sections
- Parameter Binding has conceptual explanation + concrete example + C++ snippet + flow diagram
- Element-specific tables use 3-column format with representative examples
- Help system documented briefly (mechanism only, no content listing)
- 3+ screenshot placeholders with descriptive filenames
- Cross-references to ELEMENT_REFERENCE.md, canvas.md, palette.md, layers.md, README.md
- File reads as a user-facing reference guide, not developer documentation
</verification>

<success_criteria>
- docs/manual/properties.md is 200+ lines of well-structured markdown
- PROP-01 through PROP-05 all satisfied
- Reference format (not tutorial) per CONTEXT decisions
- Common properties section with position, size, name, parameterId, lock
- Parameter Binding section with explanation, example, C++ code, flow diagram
- Element-specific tables for knob, slider, button, meter categories
- Help system subsection with (?) and F1 documentation
- 3 screenshot placeholders (overview, parameterId, help panel)
- Navigation links to README.md + cross-references
- Consistent format with getting-started.md and canvas.md
</success_criteria>

<output>
After completion, create `.planning/phases/62-properties-layers/62-01-SUMMARY.md`
</output>
