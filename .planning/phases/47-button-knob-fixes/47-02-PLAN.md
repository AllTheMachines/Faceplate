---
phase: 47-button-knob-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/elements/controls.ts
  - src/components/elements/renderers/controls/KickButtonRenderer.tsx
  - src/components/elements/renderers/controls/index.ts
  - src/components/Properties/KickButtonProperties.tsx
  - src/components/Properties/index.ts
  - src/components/Palette/Palette.tsx
  - src/components/Palette/PaletteItem.tsx
  - src/App.tsx
  - src/services/elementFactory.ts
  - src/services/export/htmlGenerator.ts
  - src/services/export/cssGenerator.ts
  - src/services/export/jsGenerator.ts
  - src/services/export/svgElementExport.ts
  - src/content/help/elements.ts
autonomous: true

must_haves:
  truths:
    - "Kick Button element type no longer exists in the system"
    - "Palette no longer shows Kick Button option"
    - "Export code no longer references kickbutton"
  artifacts:
    - path: "src/types/elements/controls.ts"
      provides: "Clean type union without KickButtonElementConfig"
      contains: "!KickButtonElementConfig"
  key_links:
    - from: "elementFactory.ts"
      to: "controls.ts"
      via: "no kickbutton case"
      pattern: "!case 'kickbutton'"
---

<objective>
Remove Kick Button element type entirely from the codebase.

Purpose: BTN-02 determined that Kick Button is redundant with Button in momentary mode. The user accepted this as a breaking change. Remove all references to simplify the element taxonomy.

Output: Codebase with no Kick Button references - cleaner types, palette, exports, and help content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-button-knob-fixes/47-CONTEXT.md

@src/types/elements/controls.ts
@src/components/elements/renderers/controls/index.ts
@src/components/Properties/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Kick Button from types and factory</name>
  <files>
    src/types/elements/controls.ts
    src/services/elementFactory.ts
  </files>
  <action>
In src/types/elements/controls.ts:
1. Delete `KickButtonElementConfig` interface (lines ~715-732)
2. Remove `KickButtonElementConfig` from the `ControlElement` union type
3. Delete `isKickButton` type guard function
4. Delete `createKickButton` factory function

In src/services/elementFactory.ts:
1. Remove any import of `createKickButton`
2. Remove the `case 'kickbutton'` from the element creation switch/map

These are the core type definitions that other files depend on.
  </action>
  <verify>TypeScript shows errors in files still referencing KickButton (expected - will fix next)</verify>
  <done>KickButtonElementConfig, isKickButton, createKickButton no longer exist in types</done>
</task>

<task type="auto">
  <name>Task 2: Remove Kick Button from renderers and properties</name>
  <files>
    src/components/elements/renderers/controls/KickButtonRenderer.tsx
    src/components/elements/renderers/controls/index.ts
    src/components/Properties/KickButtonProperties.tsx
    src/components/Properties/index.ts
  </files>
  <action>
DELETE files:
- src/components/elements/renderers/controls/KickButtonRenderer.tsx
- src/components/Properties/KickButtonProperties.tsx

UPDATE files:
In src/components/elements/renderers/controls/index.ts:
- Remove `export { KickButtonRenderer } from './KickButtonRenderer'` line
- Remove KickButtonRenderer from any renderer registry/map

In src/components/Properties/index.ts:
- Remove `export { KickButtonProperties } from './KickButtonProperties'` line
- Remove KickButtonProperties from any property panel registry/map
  </action>
  <verify>Files deleted, exports removed. TypeScript errors should decrease.</verify>
  <done>KickButtonRenderer.tsx and KickButtonProperties.tsx deleted, exports cleaned</done>
</task>

<task type="auto">
  <name>Task 3: Remove Kick Button from palette, app, exports, and help</name>
  <files>
    src/components/Palette/Palette.tsx
    src/components/Palette/PaletteItem.tsx
    src/App.tsx
    src/services/export/htmlGenerator.ts
    src/services/export/cssGenerator.ts
    src/services/export/jsGenerator.ts
    src/services/export/svgElementExport.ts
    src/content/help/elements.ts
  </files>
  <action>
Remove all Kick Button references:

1. Palette.tsx / PaletteItem.tsx:
   - Remove 'kickbutton' from palette items list
   - Remove any icon/label definitions for kickbutton

2. App.tsx:
   - Remove any kickbutton case from element type handling (if present)

3. Export generators (htmlGenerator.ts, cssGenerator.ts, jsGenerator.ts, svgElementExport.ts):
   - Remove `case 'kickbutton'` from switch statements
   - Remove any helper functions specific to kickbutton export
   - Remove imports if they reference KickButton types

4. src/content/help/elements.ts:
   - Remove kickbutton entry from help content (element descriptions, related topics)

Search for remaining references: `grep -r "kickbutton\|KickButton" src/` and clean up any missed occurrences.
  </action>
  <verify>
`npm run build` compiles without errors
`grep -ri "kickbutton" src/` returns no results
  </verify>
  <done>All Kick Button references removed from palette, exports, app, and help content</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `grep -ri "kickbutton" src/` returns no results
3. Run dev server, open palette - no Kick Button option visible
4. TypeScript: No types reference KickButtonElementConfig
5. If loading a project with old Kick Button elements - verify graceful handling (element simply won't render, no crash)
</verification>

<success_criteria>
- Zero references to kickbutton/KickButton in src/ directory
- TypeScript compiles cleanly
- Palette does not show Kick Button option
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/47-button-knob-fixes/47-02-SUMMARY.md`
</output>
