import { Canvas } from './Canvas'

/**
 * CanvasStage component - wrapper for the new HTML/CSS-based Canvas
 * Preserves backward compatibility with existing imports
 */
export function CanvasStage() {
  return <Canvas />
}
